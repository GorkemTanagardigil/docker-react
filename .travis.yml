language: generic
sudo: required
service:
  - docker

before_insatall:
  - docker build -t gtanagardigil/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gtanagardigil/docker-react npm run test -- -- coverage